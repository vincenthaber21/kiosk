# Copy to /home/dtikiosk/kiosk/.env on PythonAnywhere (not committed to git).
# Bash/manage.py reads this; the Web app uses the WSGI file env vars.

PRODUCTION=true
DEBUG=false
ALLOWED_HOSTS=dtikiosk.pythonanywhere.com
USE_SQLITE=true
DJANGO_SECRET_KEY=replace-with-a-long-random-secret-key
